# last-1
echo true | ldlmc -m last.ldl
>>>
claim holds
>>>=0

# last-2
echo "<{true}>last" | ldlmc -m last.ldl
>>>
claim does not hold
>>>=0

# test05-1
echo "<{a}>last" | ldlmc -m test05.ldl --reachability
>>>
reachable
>>>=0

# test05-2
echo "<{true}>last" | ldlmc -m test05.ldl
>>>
claim holds
>>>=0

# test16-1
echo "a" | ldlmc -m test16.ldl
>>>
claim holds
>>>=0
